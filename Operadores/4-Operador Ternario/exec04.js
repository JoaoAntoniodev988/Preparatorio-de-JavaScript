const notaAluno = 8;


// O JS olha a pergunta: 8 >= 7 ? 
// Se sim, escolhe o primeiro texto. Se não, escolhe o segundo.
const statusAluno = notaAluno >= 7 ? 'Aprovado' : 'Reprovado';
console.log(statusAluno) //Aprovado
